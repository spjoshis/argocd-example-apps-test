# Inside your values.yaml file
destination:
  namespace: default   # Ensure this block matches the structure expected by line 12

image:
  repository: myregistry/myapp
  tag: 94ad32f     # commit SHA from CI pipeline

replicaCount: 3
